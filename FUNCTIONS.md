# Simple-Video

## 機能

とにかくシンプルな動画編集アプリ。

できることは、カットと倍速、テロップのみ。

トラック式ではなく、ワンライン、1トラックのみ。

配置できるオブジェクトは3種類のみ。

- 映像
- 画像
- 単色背景

テキストは、オブジェクトのプロパティとして実装。

テキストの位置は、真ん中、下、上で固定。とにかくシンプルに。

テキストのフォント、サイズも選択式。色だけは自由に。